# 项目知识库使用指南

本指南帮助您快速掌握项目知识库的使用方法。

---

## 🚀 快速开始

### 1. 查看知识库概览

```bash
# 查看知识库索引
cat .claude/knowledge/index.yaml

# 查看知识库说明
cat .claude/knowledge/README.md

# 查看知识库统计
ls -la .claude/knowledge/errors/
```

### 2. 搜索相关知识

```bash
# 按关键词搜索
grep -r "关键词" .claude/knowledge/

# 按问题描述搜索
grep -r "问题描述" .claude/knowledge/

# 查看所有标题
grep "title:" .claude/knowledge/index.yaml
```

### 3. 查看具体知识条目

```bash
# 查看 API Schema 问题
cat .claude/knowledge/errors/api-schema-missing-field.md

# 查看服务缓存问题
cat .claude/knowledge/errors/backend-service-module-cache.md

# 查看 Windows 命令问题
cat .claude/knowledge/errors/windows-command-encoding.md
```

---

## 📚 常见使用场景

### 场景 1: API 不返回某个字段

**问题**: 数据库有数据，但 API 响应中没有该字段

**查找方法**:
```bash
grep -r "API.*字段" .claude/knowledge/
# 或
grep -r "Pydantic" .claude/knowledge/
```

**找到**: KB-P-001 - API Schema 缺少字段导致数据不返回

**解决方案**:
1. 打开 `backend/schemas/product_schema.py`
2. 在 Response Schema 中添加缺失的字段
3. 重启后端服务
4. 验证 API 响应

---

### 场景 2: 代码修改后不生效

**问题**: 修改了代码，重启服务，但更新不生效

**查找方法**:
```bash
grep -r "代码更新不生效" .claude/knowledge/
# 或
grep -r "模块缓存" .claude/knowledge/
```

**找到**: KB-P-002 - 后端服务模块缓存导致代码更新不生效

**解决方案**:
1. 检查端口占用: `netstat -ano | findstr :8001`
2. 关闭所有 Python 进程
3. 验证端口已释放
4. 重新启动服务
5. 验证只有一个进程在运行

---

### 场景 3: Windows 命令报错

**问题**: 使用 taskkill 等命令时出现编码错误

**查找方法**:
```bash
grep -r "taskkill" .claude/knowledge/
# 或
grep -r "Windows.*编码" .claude/knowledge/
```

**找到**: KB-P-003 - Windows 命令编码问题

**解决方案**:
1. 在 Git Bash 中使用 `//` 代替 `/`
2. 或使用 Python subprocess 处理编码
3. 或使用端口管理工具

---

## 🔍 高级搜索技巧

### 1. 按分类搜索

```bash
# 搜索所有错误记录
ls .claude/knowledge/errors/

# 搜索所有解决方案
ls .claude/knowledge/solutions/

# 搜索所有代码模式
ls .claude/knowledge/patterns/
```

### 2. 按关键词搜索

```bash
# 搜索 FastAPI 相关知识
grep -r "FastAPI" .claude/knowledge/

# 搜索 Pydantic 相关知识
grep -r "Pydantic" .claude/knowledge/

# 搜索端口相关知识
grep -r "端口" .claude/knowledge/
```

### 3. 按文件名搜索

```bash
# 查找包含 "api" 的文件
find .claude/knowledge -name "*api*"

# 查找包含 "cache" 的文件
find .claude/knowledge -name "*cache*"
```

### 4. 查看相关知识

每个知识条目都有 "相关知识" 部分，可以找到相关的其他条目：

```bash
# 查看 KB-P-001 的相关知识
grep -A 5 "## 相关知识" .claude/knowledge/errors/api-schema-missing-field.md
```

---

## 💡 使用技巧

### 1. 快速定位问题

当遇到问题时，按以下顺序搜索：

1. **按错误信息搜索**
   ```bash
   grep -r "错误信息" .claude/knowledge/
   ```

2. **按关键词搜索**
   ```bash
   grep -r "关键词" .claude/knowledge/
   ```

3. **按分类浏览**
   ```bash
   ls .claude/knowledge/errors/
   ```

4. **查看索引**
   ```bash
   cat .claude/knowledge/index.yaml
   ```

### 2. 复制解决方案

知识条目中的代码可以直接复制使用：

```bash
# 查看完整的解决方案代码
cat .claude/knowledge/errors/api-schema-missing-field.md | grep -A 20 "```python"
```

### 3. 学习预防措施

每个知识条目都包含预防措施，帮助避免再次遇到同样的问题：

```bash
# 查看预防措施
grep -A 20 "## 预防措施" .claude/knowledge/errors/*.md
```

---

## 📊 知识库维护

### 添加新知识

当解决了新问题后，使用命令添加到知识库：

```bash
/add-knowledge-project
```

系统会自动：
1. 分析上下文
2. 提取问题和解决方案
3. 生成知识条目
4. 更新索引

### 更新现有知识

如果发现更好的解决方案，可以手动更新知识条目：

```bash
# 编辑知识条目
vim .claude/knowledge/errors/api-schema-missing-field.md

# 更新索引中的 updated 时间
vim .claude/knowledge/index.yaml
```

### 删除过时知识

如果某个知识条目不再适用：

```bash
# 删除知识条目文件
rm .claude/knowledge/errors/obsolete-knowledge.md

# 从索引中删除对应条目
vim .claude/knowledge/index.yaml
```

---

## 🎯 最佳实践

### 1. 及时记录

- ✅ 问题解决后立即添加知识条目
- ✅ 记录完整的解决过程
- ✅ 包含实际的代码示例

### 2. 详细描述

- ✅ 提供足够的上下文
- ✅ 说明问题的根本原因
- ✅ 列出多种解决方案

### 3. 定期回顾

- ✅ 每月回顾知识库
- ✅ 更新过时的信息
- ✅ 添加新的使用案例

### 4. 团队共享

- ✅ 向团队成员介绍知识库
- ✅ 鼓励大家添加知识
- ✅ 定期分享有价值的知识条目

---

## 🔗 相关资源

- **知识库索引**: `.claude/knowledge/index.yaml`
- **知识库说明**: `.claude/knowledge/README.md`
- **项目文档**: `docs/`
- **测试报告**: `docs/翻译功能测试报告.md`

---

## 📞 获取帮助

如果您在使用知识库时遇到问题：

1. 查看本使用指南
2. 查看知识库 README
3. 搜索相关知识条目
4. 向团队成员咨询

---

**版本**: 1.0.0
**创建日期**: 2026-01-29
**最后更新**: 2026-01-29
