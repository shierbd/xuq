# ====================
# MVP词根聚类需求挖掘系统 - 配置模板
# ====================
# 复制此文件为 .env 并填入实际值

# ====================
# 数据库配置
# ====================
# 数据库类型: mysql 或 sqlite
DB_TYPE=mysql

# MySQL/MariaDB配置（推荐用于生产环境）
DB_HOST=localhost
DB_PORT=3306
DB_NAME=keyword_clustering
DB_USER=root
DB_PASSWORD=your_mysql_password_here

# ====================
# LLM配置
# ====================
# LLM提供商选择: openai, anthropic, deepseek
LLM_PROVIDER=openai

# OpenAI配置（推荐，gpt-4o-mini性价比高）
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1

# Anthropic配置（可选，质量最高但较贵）
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# DeepSeek配置（可选，最便宜）
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# ====================
# 日志配置
# ====================
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ====================
# 配置说明
# ====================
#
# 1. 数据库配置：
#    - 开发/测试: 使用SQLite (DB_TYPE=sqlite)
#    - 生产环境: 使用MySQL/MariaDB (DB_TYPE=mysql)
#
# 2. LLM提供商选择：
#    - OpenAI (gpt-4o-mini): $0.15/1M tokens，推荐
#    - Anthropic (claude-3.5): $3/1M tokens，质量最高
#    - DeepSeek: $0.14/1M tokens，最便宜
#
# 3. API成本估算（55K短语完整运行）：
#    - Phase 3 (主题生成): ~$0.06
#    - Phase 4 (需求卡片): ~$0.30
#    - Phase 5 (Token分类): ~$0.04
#    - 总计: ~$0.40 (使用gpt-4o-mini)
#
# 4. 安全提示：
#    - ⚠️ 不要将 .env 文件提交到Git
#    - ⚠️ .gitignore 已配置忽略 .env
#    - ⚠️ 请妥善保管API密钥
#
# 5. 快速开始：
#    1) 复制: cp .env.example .env (Windows: copy .env.example .env)
#    2) 编辑 .env 填写数据库密码和API密钥
#    3) 初始化数据库: python init_database.py
#    4) 启动Web界面: python web_ui.py
#    5) 浏览器打开: http://localhost:8501
#
