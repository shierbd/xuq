# 步骤A3：语义聚类 - 详细讲解

## 🎯 为什么需要聚类？

### 问题背景

经过步骤A2，你现在有**6,565条关键词短语**，来自**46个种子词**。

```
compress pdf online
compress image without losing quality
pdf compressor
compress video
video compressor
mp4 compressor
...
```

**问题**：
- 这些短语太多、太杂，无法直接分析
- 很多短语表达的是**相同的需求**（如 "compress pdf" 和 "pdf compressor"）
- 需要找到**潜在的需求方向**（如 PDF压缩、图片压缩、视频压缩）

**解决方案**：
用**语义聚类**把相似的短语自动归类到一起，形成"需求簇"。

---

## 🧠 核心原理：从文字到数字

### 第1步：Embedding（词嵌入）

**原理**：把文字转换成数字向量（一串数字），语义相似的文字会有相似的向量。

```
文字                        →  向量（简化示例）
─────────────────────────────────────────────
"compress pdf online"       →  [0.2, 0.8, 0.1, 0.5, ...]
"pdf compressor"            →  [0.3, 0.7, 0.2, 0.4, ...]  ← 很接近！
"video editor"              →  [0.9, 0.1, 0.8, 0.2, ...]  ← 很远！
```

**实际维度**：384维（all-MiniLM-L6-v2模型）

**比喻**：
- 就像给每个短语一个"身份证号码"
- 意思相近的短语，"号码"也相近
- 可以用数学方法计算相似度

### 第2步：聚类（Clustering）

**原理**：把向量空间中相近的点归为一组。

```
        ┌─────────────────────┐
        │  PDF压缩相关         │
        │  • compress pdf     │
        │  • pdf compressor   │
        │  • reduce pdf size  │
        └─────────────────────┘
                 ↓
            簇ID: 5


        ┌─────────────────────┐
        │  视频压缩相关         │
        │  • compress video   │
        │  • video compressor │
        │  • reduce video size│
        └─────────────────────┘
                 ↓
            簇ID: 12
```

---

## 🔬 技术实现细节

### 使用的技术栈

#### 1. **Sentence-Transformers** (Embedding模型)

**模型**：`all-MiniLM-L6-v2`
- **大小**：约90MB
- **速度**：快速（CPU上也能跑）
- **输出维度**：384维向量

**为什么选它**？
- ✅ 专门针对语义相似度优化
- ✅ 支持短文本（关键词）
- ✅ 轻量级，适合大规模数据

**代码实现**：
```python
from sentence_transformers import SentenceTransformer

# 加载模型
model = SentenceTransformer('all-MiniLM-L6-v2')

# 生成embedding
phrases = ["compress pdf", "pdf compressor", "video editor"]
embeddings = model.encode(phrases)

# 结果：
# embeddings.shape = (3, 384)
# 每行是一个384维的向量
```

#### 2. **HDBSCAN** (聚类算法)

**全称**：Hierarchical Density-Based Spatial Clustering of Applications with Noise

**优点**：
- ✅ 自动确定簇的数量（不需要提前指定）
- ✅ 能识别噪音点（不属于任何簇的点）
- ✅ 能处理不同大小和密度的簇

**核心参数**：

1. **min_cluster_size**：最小簇大小
   ```python
   min_cluster_size = 5
   # 含义：一个簇至少要有5个点
   # 调大 → 簇更少、更大
   # 调小 → 簇更多、更小
   ```

2. **min_samples**：核心点的最小邻居数
   ```python
   min_samples = 2
   # 含义：一个点要成为核心点，至少需要2个邻居
   # 调大 → 更严格，噪音点更多
   # 调小 → 更宽松，几乎所有点都会被分类
   ```

**代码实现**：
```python
import hdbscan

clusterer = hdbscan.HDBSCAN(
    min_cluster_size=5,
    min_samples=2,
    metric='euclidean'
)

cluster_labels = clusterer.fit_predict(embeddings)
# 结果：[5, 5, 12, 5, 12, -1, ...]
# -1 表示噪音点
```

---

## 📊 步骤A3的完整流程

### 流程图

```
[merged_keywords_all.csv]
        ↓
   加载数据
   6,565条短语
        ↓
   数据预处理
   （可选：过滤低搜索量）
        ↓
   生成Embeddings
   6,565 × 384 维矩阵
        ↓
   HDBSCAN聚类
   计算簇标签
        ↓
   添加cluster_id列
   保存到CSV
        ↓
   计算簇级统计
   每个簇的大小、频次等
        ↓
[stageA_clusters.csv]
[clusters_summary_stageA.csv]
```

### 代码逻辑分解

#### **步骤1：加载数据**
```python
df = pd.read_csv('merged_keywords_all.csv')

# 数据示例：
#   Keyword            seed_word  Volume  frequency
#   compress pdf       compress   12100   3
#   pdf compressor     compress   8100    2
#   video editor       edit       5400    1
```

#### **步骤2：数据预处理**
```python
# 可选：过滤低搜索量
if config['min_volume'] > 0:
    df = df[df['Volume'] >= config['min_volume']]

# 可选：限制数量（节省时间）
if config['max_phrases']:
    df = df.nlargest(config['max_phrases'], 'Volume')

phrases = df['phrase'].tolist()
```

#### **步骤3：生成Embeddings**
```python
model = SentenceTransformer('all-MiniLM-L6-v2')

# 批量生成，显示进度条
embeddings = model.encode(
    phrases,
    batch_size=32,
    show_progress_bar=True
)

# embeddings: (6565, 384) 的numpy数组
```

**时间**：
- 6,565条短语
- CPU：约5-10分钟
- GPU：约1-2分钟

#### **步骤4：聚类**
```python
clusterer = hdbscan.HDBSCAN(
    min_cluster_size=5,
    min_samples=2,
    metric='euclidean'
)

cluster_labels = clusterer.fit_predict(embeddings)

# 结果示例：
# [5, 5, 12, 5, 12, -1, 3, 3, 3, ...]
# 数字代表簇ID，-1代表噪音点
```

#### **步骤5：保存结果**
```python
# 添加簇标签到DataFrame
df['cluster_id'] = cluster_labels

# 保存
df.to_csv('stageA_clusters.csv', index=False)
```

**输出文件格式**：
```csv
phrase,seed_word,Volume,frequency,cluster_id
compress pdf,compress,12100,3,5
pdf compressor,compress,8100,2,5
compress video,compress,5400,1,12
video compressor,compress,4100,1,12
random phrase,other,100,1,-1
```

#### **步骤6：簇级统计**
```python
summary = df.groupby('cluster_id').agg({
    'phrase': 'count',           # 簇大小
    'frequency': 'sum',          # 总频次
    'seed_word': lambda x: ','.join(set(x)),  # 种子词
    'Volume': 'sum'              # 总搜索量
})

# 输出：clusters_summary_stageA.csv
```

**输出格式**：
```csv
cluster_id,cluster_size,total_frequency,seed_words_in_cluster,total_volume
5,25,150,compress,250000
12,18,90,compress,180000
3,30,200,"compress,convert",300000
```

---

## 🎨 可视化理解

### 二维投影示例（简化）

```
     ┌─────────────────────────────────────┐
     │                                     │
     │        ●●●  簇5 (PDF压缩)           │
     │       ●●●●                          │
     │                                     │
     │                  ●                  │
     │                 ● ● 噪音点          │
     │                  ●                  │
     │                                     │
     │                      ▲▲▲            │
     │                     ▲▲▲▲ 簇12       │
     │                      ▲▲  (视频压缩) │
     │                                     │
     │  ■■■                                │
     │ ■■■■ 簇3 (图片编辑)                 │
     │  ■■                                 │
     │                                     │
     └─────────────────────────────────────┘
```

实际是384维空间，这里简化为2维展示。

---

## ⚙️ 参数调优指南

### **min_cluster_size**（最小簇大小）

**作用**：控制簇的数量和大小

| 值 | 效果 | 适用场景 |
|----|------|---------|
| 3 | 簇很多、很小 | 数据量小（<1000条） |
| 5 | 中等数量的簇 | **推荐默认值** |
| 10 | 簇较少、较大 | 数据量大（>5000条） |
| 20 | 簇很少、很大 | 只想要主要方向 |

**调整建议**：
```python
# 如果聚类结果簇太多（>100个）
min_cluster_size = 10  # 增大

# 如果簇太少（<10个）
min_cluster_size = 3   # 减小
```

### **min_samples**（最小邻居数）

**作用**：控制噪音点的比例

| 值 | 效果 | 噪音点比例 |
|----|------|----------|
| 1 | 几乎没有噪音点 | <5% |
| 2 | 少量噪音点 | **推荐默认值**，5-15% |
| 5 | 较多噪音点 | 20-30% |
| 10 | 很多噪音点 | >40% |

**调整建议**：
```python
# 如果噪音点太多（>30%）
min_samples = 1  # 减小

# 如果想过滤更多杂乱短语
min_samples = 5  # 增大
```

---

## 📈 预期结果

### 典型输出

**数据规模**：6,565条短语

**聚类结果**（参数：min_cluster_size=5, min_samples=2）：
- **簇数量**：约40-60个
- **噪音点**：约10-20%
- **簇大小分布**：
  - 大簇（>50条）：5-10个
  - 中簇（10-50条）：20-30个
  - 小簇（5-10条）：10-20个

### 簇的例子

**簇5：PDF压缩工具**（25条短语）
```
compress pdf online
pdf compressor
reduce pdf size
shrink pdf file
compress pdf free
pdf compression tool
...
```

**簇12：视频压缩工具**（18条短语）
```
compress video
video compressor
reduce video size
compress mp4
video compression software
...
```

**簇-1：噪音点**（约800条）
```
random unrelated phrase
super specific long tail query
...
```

---

## 🔍 如何验证聚类质量？

### 方法1：查看簇内样本

```python
# 读取结果
df = pd.read_csv('stageA_clusters.csv')

# 查看簇5的所有短语
cluster_5 = df[df['cluster_id'] == 5]
print(cluster_5['phrase'].tolist())
```

**好的聚类**：簇内短语语义相关、主题一致

**差的聚类**：簇内短语杂乱、没有明显主题

### 方法2：查看簇统计

```python
summary = pd.read_csv('clusters_summary_stageA.csv')

# Top 10最大的簇
print(summary.head(10))
```

**好的聚类**：
- 大簇有明确主题
- 中小簇覆盖细分方向
- 噪音点比例合理（10-20%）

**差的聚类**：
- 大簇混杂多个主题
- 大量小簇（<5条）
- 噪音点过多（>30%）

---

## 🎯 下一步：簇级理解（步骤A4）

聚类完成后，每个簇只有一个数字ID（如 cluster_id=5）。

**步骤A4的任务**：
- 用大模型给每个簇起一个**人类可读的名字**
- 生成**簇的描述**（这个簇的短语在说什么需求）

**示例**：
```json
{
  "cluster_id": 5,
  "cluster_title": "PDF文件压缩与体积减小",
  "short_label": "compress_pdf",
  "scope_description": "用户希望压缩PDF文档体积，用于上传、分享、邮件发送等场景"
}
```

---

## 💡 常见问题

### Q1: 为什么要先做embedding再聚类？

**A**: 因为文字本身无法直接计算相似度。

```
"compress pdf" vs "pdf compressor"
```
- 字面上完全不同（无法匹配）
- 但语义相同（都是PDF压缩）
- Embedding能捕捉语义相似度

### Q2: 可以用KMeans代替HDBSCAN吗？

**A**: 可以，但需要提前指定簇数量。

```python
# config.py
A3_CONFIG = {
    "clustering_method": "kmeans",
    "n_clusters": 50,  # 必须指定
}
```

**HDBSCAN vs KMeans**：

| 特性 | HDBSCAN | KMeans |
|------|---------|--------|
| 簇数量 | 自动确定 | 必须指定 |
| 噪音点 | 能识别 | 不能识别 |
| 速度 | 较慢 | 较快 |
| 推荐 | ✅ 探索阶段 | 知道要多少簇 |

### Q3: 聚类需要多长时间？

**A**: 取决于数据量和硬件。

| 数据量 | CPU | GPU |
|-------|-----|-----|
| 1,000条 | 1-2分钟 | 30秒 |
| 5,000条 | 5-10分钟 | 1-2分钟 |
| 10,000条 | 15-20分钟 | 3-5分钟 |

**提速方法**：
1. 减少数据量（过滤低搜索量）
2. 使用GPU（设置 `use_gpu=True`）
3. 使用更小的模型

### Q4: 聚类结果不满意怎么办？

**A**: 调整参数重新运行。

**簇太多**：
```python
min_cluster_size = 10  # 增大
```

**簇太少**：
```python
min_cluster_size = 3   # 减小
```

**噪音点太多**：
```python
min_samples = 1  # 减小
```

---

## 🎓 总结

### 核心概念

1. **Embedding** = 把文字变成数字向量
2. **聚类** = 把相似的向量归为一组
3. **簇** = 一组语义相关的短语
4. **簇ID** = 每个簇的唯一标识

### 技术栈

- **sentence-transformers** → Embedding
- **HDBSCAN** → 聚类
- **pandas** → 数据处理

### 输出

- `stageA_clusters.csv` → 带簇标签的短语
- `clusters_summary_stageA.csv` → 簇统计

### 意义

从**6,565条杂乱短语** → **40-60个需求方向簇**

---

## 🚀 现在可以运行了

```bash
python step_A3_clustering.py
```

预计耗时：5-10分钟

有任何问题随时问我！
