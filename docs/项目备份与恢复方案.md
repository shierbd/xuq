# 项目备份与恢复方案

**创建时间**: 2026-02-01
**文档版本**: v1.0
**目标**: 提供完整的项目备份和恢复方案，确保架构迁移时可以随时回滚

---

## 🎯 备份目标

### 需要备份的内容

1. **代码** - 所有源代码和配置文件
2. **数据** - 数据库、CSV文件、缓存数据
3. **依赖** - Python和Node.js依赖清单
4. **环境** - 环境变量和配置
5. **文档** - 所有文档和说明

### 恢复要求

- ✅ 可以完整恢复到备份时的状态
- ✅ 所有功能正常工作
- ✅ 数据完整无损
- ✅ 恢复时间 < 10分钟

---

## 📦 备份方案

### 方案1：Git + 数据备份（推荐）⭐⭐⭐⭐⭐

**优点**:
- 版本控制，可以回到任意时间点
- 轻量级，只备份变更
- 支持多个备份点
- 可以对比不同版本

**步骤**:

#### 1. 创建备份分支
```bash
# 创建备份分支（带时间戳）
git checkout -b backup-before-htmx-migration-20260201

# 提交所有当前更改
git add .
git commit -m "backup: 完整备份 - 迁移到HTMX之前

当前状态:
- 架构: React + FastAPI
- 代码行数: 27,105行
- 功能: 完整可用
- 数据: 15,792条商品，1,411个聚类簇

备份原因: 准备迁移到FastAPI+HTMX架构
备份时间: 2026-02-01

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"

# 推送到远程（如果有）
git push origin backup-before-htmx-migration-20260201
```

#### 2. 备份数据文件
```bash
# 创建备份目录
mkdir -p backups/backup-20260201

# 备份数据库
cp data/products.db backups/backup-20260201/products.db

# 备份所有数据文件
cp -r data/ backups/backup-20260201/data/

# 备份环境配置
cp .env backups/backup-20260201/.env

# 创建备份清单
echo "备份时间: $(date)" > backups/backup-20260201/BACKUP_INFO.txt
echo "Git分支: backup-before-htmx-migration-20260201" >> backups/backup-20260201/BACKUP_INFO.txt
echo "Git提交: $(git rev-parse HEAD)" >> backups/backup-20260201/BACKUP_INFO.txt
```

#### 3. 备份依赖清单
```bash
# 备份Python依赖
pip freeze > backups/backup-20260201/requirements-frozen.txt

# 备份Node.js依赖（如果有）
cd frontend
npm list --depth=0 > ../backups/backup-20260201/npm-packages.txt
cd ..
```

#### 4. 创建压缩包（可选）
```bash
# 压缩备份目录
tar -czf backups/backup-20260201.tar.gz backups/backup-20260201/

# 或使用zip（Windows友好）
# powershell: Compress-Archive -Path backups/backup-20260201 -DestinationPath backups/backup-20260201.zip
```

---

### 方案2：完整项目复制（最简单）⭐⭐⭐⭐

**优点**:
- 最简单，直接复制整个项目
- 不需要Git知识
- 恢复最快

**缺点**:
- 占用空间大（~500MB）
- 不支持版本对比

**步骤**:

```bash
# Windows PowerShell
Copy-Item -Path "D:\xiangmu\词根聚类需求挖掘" -Destination "D:\xiangmu\词根聚类需求挖掘-backup-20260201" -Recurse

# 或者手动复制整个文件夹
# 右键 -> 复制 -> 粘贴 -> 重命名为 "词根聚类需求挖掘-backup-20260201"
```

---

### 方案3：混合方案（最安全）⭐⭐⭐⭐⭐

**结合方案1和方案2**:
1. 使用Git创建备份分支（代码版本控制）
2. 复制整个项目文件夹（完整备份）
3. 单独备份数据文件（数据安全）

---

## 🔄 恢复方案

### 从Git备份恢复

#### 步骤1：切换到备份分支
```bash
# 查看所有分支
git branch -a

# 切换到备份分支
git checkout backup-before-htmx-migration-20260201

# 确认当前状态
git log -1
git status
```

#### 步骤2：恢复数据文件
```bash
# 恢复数据库
cp backups/backup-20260201/products.db data/products.db

# 恢复所有数据文件
cp -r backups/backup-20260201/data/ data/

# 恢复环境配置
cp backups/backup-20260201/.env .env
```

#### 步骤3：恢复依赖
```bash
# 恢复Python依赖
pip install -r backups/backup-20260201/requirements-frozen.txt

# 恢复Node.js依赖
cd frontend
npm install
cd ..
```

#### 步骤4：验证恢复
```bash
# 启动后端
python -m uvicorn api.main:app --reload --port 8000

# 启动前端（新终端）
cd frontend
npm run dev

# 访问 http://localhost:5173 验证功能
```

---

### 从完整项目复制恢复

#### 步骤1：删除当前项目（可选）
```bash
# 如果要完全替换
# 先确认备份存在！
cd D:\xiangmu
mv 词根聚类需求挖掘 词根聚类需求挖掘-failed-migration
```

#### 步骤2：恢复备份
```bash
# 复制备份回来
Copy-Item -Path "D:\xiangmu\词根聚类需求挖掘-backup-20260201" -Destination "D:\xiangmu\词根聚类需求挖掘" -Recurse
```

#### 步骤3：验证恢复
```bash
cd 词根聚类需求挖掘

# 启动服务验证
python -m uvicorn api.main:app --reload --port 8000
```

---

## 🛠️ 自动化备份脚本

### backup.py - Python备份脚本

```python
#!/usr/bin/env python3
"""
项目备份脚本
用法: python scripts/backup.py
"""

import os
import shutil
import subprocess
from datetime import datetime
from pathlib import Path

def create_backup():
    """创建完整备份"""

    # 1. 生成时间戳
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    backup_name = f"backup-{timestamp}"
    backup_dir = Path(f"backups/{backup_name}")

    print(f"🔄 开始备份: {backup_name}")

    # 2. 创建备份目录
    backup_dir.mkdir(parents=True, exist_ok=True)

    # 3. 备份数据库
    print("📦 备份数据库...")
    if Path("data/products.db").exists():
        shutil.copy2("data/products.db", backup_dir / "products.db")

    # 4. 备份数据目录
    print("📦 备份数据文件...")
    if Path("data").exists():
        shutil.copytree("data", backup_dir / "data", dirs_exist_ok=True)

    # 5. 备份环境配置
    print("📦 备份环境配置...")
    if Path(".env").exists():
        shutil.copy2(".env", backup_dir / ".env")

    # 6. 备份依赖清单
    print("📦 备份依赖清单...")
    subprocess.run(["pip", "freeze"],
                   stdout=open(backup_dir / "requirements-frozen.txt", "w"))

    # 7. 创建Git备份分支
    print("📦 创建Git备份分支...")
    try:
        branch_name = f"backup-{timestamp}"
        subprocess.run(["git", "checkout", "-b", branch_name])
        subprocess.run(["git", "add", "."])
        subprocess.run(["git", "commit", "-m", f"backup: 自动备份 {timestamp}"])
        print(f"✅ Git分支已创建: {branch_name}")
    except Exception as e:
        print(f"⚠️ Git备份失败: {e}")

    # 8. 创建备份信息文件
    with open(backup_dir / "BACKUP_INFO.txt", "w", encoding="utf-8") as f:
        f.write(f"备份时间: {datetime.now()}\n")
        f.write(f"备份名称: {backup_name}\n")
        try:
            commit = subprocess.check_output(["git", "rev-parse", "HEAD"]).decode().strip()
            f.write(f"Git提交: {commit}\n")
        except:
            pass

    # 9. 创建压缩包
    print("📦 创建压缩包...")
    shutil.make_archive(f"backups/{backup_name}", "zip", backup_dir)

    print(f"✅ 备份完成: backups/{backup_name}.zip")
    print(f"📁 备份目录: {backup_dir}")

    return backup_name

if __name__ == "__main__":
    create_backup()
```

### restore.py - Python恢复脚本

```python
#!/usr/bin/env python3
"""
项目恢复脚本
用法: python scripts/restore.py backup-20260201_143022
"""

import os
import shutil
import subprocess
import sys
from pathlib import Path

def restore_backup(backup_name):
    """从备份恢复"""

    backup_dir = Path(f"backups/{backup_name}")

    if not backup_dir.exists():
        print(f"❌ 备份不存在: {backup_dir}")
        return False

    print(f"🔄 开始恢复: {backup_name}")

    # 1. 读取备份信息
    info_file = backup_dir / "BACKUP_INFO.txt"
    if info_file.exists():
        print("\n📋 备份信息:")
        print(info_file.read_text(encoding="utf-8"))

    # 2. 确认恢复
    confirm = input("\n⚠️ 确认要恢复此备份吗？当前数据将被覆盖！(yes/no): ")
    if confirm.lower() != "yes":
        print("❌ 恢复已取消")
        return False

    # 3. 恢复数据库
    print("\n📦 恢复数据库...")
    if (backup_dir / "products.db").exists():
        shutil.copy2(backup_dir / "products.db", "data/products.db")

    # 4. 恢复数据目录
    print("📦 恢复数据文件...")
    if (backup_dir / "data").exists():
        if Path("data").exists():
            shutil.rmtree("data")
        shutil.copytree(backup_dir / "data", "data")

    # 5. 恢复环境配置
    print("📦 恢复环境配置...")
    if (backup_dir / ".env").exists():
        shutil.copy2(backup_dir / ".env", ".env")

    # 6. 恢复依赖
    print("📦 恢复Python依赖...")
    if (backup_dir / "requirements-frozen.txt").exists():
        subprocess.run(["pip", "install", "-r",
                       str(backup_dir / "requirements-frozen.txt")])

    print("\n✅ 恢复完成！")
    print("\n🚀 下一步:")
    print("1. 启动后端: python -m uvicorn api.main:app --reload --port 8000")
    print("2. 启动前端: cd frontend && npm run dev")
    print("3. 访问: http://localhost:5173")

    return True

def list_backups():
    """列出所有备份"""
    backups_dir = Path("backups")
    if not backups_dir.exists():
        print("❌ 没有找到备份目录")
        return

    backups = sorted([d.name for d in backups_dir.iterdir() if d.is_dir()])

    if not backups:
        print("❌ 没有找到任何备份")
        return

    print("\n📦 可用的备份:")
    for i, backup in enumerate(backups, 1):
        info_file = backups_dir / backup / "BACKUP_INFO.txt"
        if info_file.exists():
            first_line = info_file.read_text(encoding="utf-8").split("\n")[0]
            print(f"{i}. {backup} - {first_line}")
        else:
            print(f"{i}. {backup}")

if __name__ == "__main__":
    if len(sys.argv) < 2:
        print("用法: python scripts/restore.py <backup_name>")
        print("\n或者: python scripts/restore.py --list  # 列出所有备份")
        list_backups()
        sys.exit(1)

    if sys.argv[1] == "--list":
        list_backups()
    else:
        restore_backup(sys.argv[1])
```

---

## ✅ 备份验证清单

### 备份前检查

- [ ] 所有代码已提交到Git
- [ ] 数据库文件存在（data/products.db）
- [ ] 环境配置文件存在（.env）
- [ ] 当前功能正常工作

### 备份后检查

- [ ] 备份分支已创建
- [ ] 备份目录已创建
- [ ] 数据文件已备份
- [ ] 压缩包已创建
- [ ] 备份信息文件已创建

### 恢复后验证

- [ ] 后端可以启动
- [ ] 前端可以启动
- [ ] 可以访问首页
- [ ] 商品列表可以加载
- [ ] 聚类功能可以使用
- [ ] AI功能可以使用
- [ ] 数据完整（15,792条商品）

---

## 🚨 紧急恢复指南

### 如果迁移失败，快速恢复

#### 最快方法（5分钟）

```bash
# 1. 切换到备份分支
git checkout backup-before-htmx-migration-20260201

# 2. 恢复数据
cp backups/backup-20260201/products.db data/products.db

# 3. 启动服务
python -m uvicorn api.main:app --reload --port 8000

# 完成！
```

#### 完整恢复（10分钟）

```bash
# 1. 运行恢复脚本
python scripts/restore.py backup-20260201

# 2. 按提示操作

# 完成！
```

---

## 📝 备份最佳实践

### 1. 备份时机

- ✅ 架构迁移前（必须）
- ✅ 重大功能开发前
- ✅ 数据库结构变更前
- ✅ 依赖升级前
- ✅ 每周定期备份

### 2. 备份命名

使用清晰的命名规则：
```
backup-before-htmx-migration-20260201  # 迁移前备份
backup-before-db-migration-20260205    # 数据库迁移前
backup-weekly-20260208                 # 每周备份
```

### 3. 备份存储

- ✅ 本地备份（backups/目录）
- ✅ Git远程仓库（GitHub/GitLab）
- ✅ 云存储（可选，如OneDrive/Google Drive）
- ✅ 外部硬盘（重要项目）

### 4. 备份保留

- 迁移前备份：永久保留
- 每周备份：保留4周
- 每日备份：保留7天

---

## 🎯 推荐方案

### 对于本次HTMX迁移

**推荐使用：方案3（混合方案）**

#### 执行步骤

```bash
# 1. 创建Git备份分支
git checkout -b backup-before-htmx-migration-20260201
git add .
git commit -m "backup: 完整备份 - 迁移到HTMX之前"

# 2. 运行自动备份脚本
python scripts/backup.py

# 3. 手动复制整个项目（双保险）
# 复制 "词根聚类需求挖掘" 到 "词根聚类需求挖掘-backup-20260201"

# 4. 验证备份
ls backups/
git branch
```

#### 恢复测试

```bash
# 测试恢复流程（在测试目录）
python scripts/restore.py backup-20260201
```

---

## 📞 故障排查

### 问题1：Git备份失败

**原因**: 有未提交的更改

**解决**:
```bash
git status
git add .
git commit -m "临时提交"
```

### 问题2：数据恢复后不完整

**原因**: 备份时数据库正在使用

**解决**:
- 备份前先停止所有服务
- 使用数据库导出工具

### 问题3：依赖安装失败

**原因**: 依赖版本冲突

**解决**:
```bash
# 使用虚拟环境
python -m venv venv-restore
source venv-restore/bin/activate  # Windows: venv-restore\Scripts\activate
pip install -r requirements-frozen.txt
```

---

## 总结

### 推荐备份流程

1. **迁移前**: 运行 `python scripts/backup.py`
2. **迁移中**: 保持备份不动
3. **迁移后**: 验证新系统，确认无问题后可以删除旧备份

### 恢复保证

- ✅ 5分钟内可以恢复到备份状态
- ✅ 数据完整无损
- ✅ 功能完全正常

### 风险控制

- ✅ 多重备份（Git + 文件 + 数据）
- ✅ 自动化脚本（减少人为错误）
- ✅ 验证清单（确保备份完整）

---

**文档创建者**: Claude Sonnet 4.5
**创建日期**: 2026-02-01
**文档状态**: ✅ 完整

---

*现在可以安全地进行架构迁移了！*
