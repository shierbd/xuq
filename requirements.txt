# ==================== MVP版本核心依赖 ====================
# Python版本要求: >= 3.8

# 数据处理
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0  # Excel文件读写

# 数据库
sqlalchemy>=2.0.0
pymysql>=1.1.0  # MySQL连接器
cryptography>=41.0.0  # PyMySQL依赖

# 环境变量管理
python-dotenv>=1.0.0

# ==================== 机器学习 ====================
# 文本向量化
sentence-transformers>=2.2.0
torch>=2.0.0  # sentence-transformers依赖

# 聚类算法
scikit-learn>=1.3.0
hdbscan>=0.8.33

# ==================== LLM集成 ====================
# OpenAI
openai>=1.0.0

# Anthropic (可选)
anthropic>=0.8.0

# HTTP请求
requests>=2.31.0
httpx>=0.25.0

# ==================== 工具库 ====================
# 进度条
tqdm>=4.66.0

# 编码检测
chardet>=3.0.4

# 数据验证（可选）
pydantic>=2.0.0

# 自然语言处理（Phase 0词性标注）
nltk>=3.8.0

# 翻译服务（Phase 0，已解决依赖冲突）
deep-translator>=1.11.0  # 使用deep-translator替代googletrans，无依赖冲突

# ==================== Web UI ====================
# Streamlit Web界面
streamlit>=1.28.0

# 词云生成（可选，Phase 5使用）
wordcloud>=1.9.0
matplotlib>=3.7.0

# ==================== 注释：暂不包含的依赖 ====================
# 以下依赖在MVP阶段不需要，后续迭代再添加：
# - seaborn (Phase 6可视化时添加)
# - pytrends (Google Trends，暂不用)
# - beautifulsoup4 (Web抓取，暂不用)
# - transformers (sentence-transformers已包含)
# - jupyterlab (数据探索，可选)
# - fastapi (Web API，Phase 3完整版UI时添加)
